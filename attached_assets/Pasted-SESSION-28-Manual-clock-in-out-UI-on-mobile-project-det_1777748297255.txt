SESSION 28 — Manual clock-in/out UI on mobile project detail screen.

Mobile only. No backend changes. No geofencing yet (that's Session 30+).

Backend already shipped (Sessions 22-26):
- POST /api/timesheets/clock-in { projectId, notes? } → 201 entry
- POST /api/timesheets/clock-out { notes? } → 200 closed entry
- GET /api/timesheets/active → 200 entry|null
- All cookie-authenticated, mobile-verified in Session 27.5
- Cookie jar already in services/api.ts

DIAGNOSE FIRST — DO NOT WRITE CODE YET

Answer all 5 items. Don't propose code yet.

1. Project detail screen
   - Confirm path is artifacts/mobile/app/project/[id].tsx (~1876 lines)
   - Identify where the page header / tab bar / content area is
   - Where would a sticky bottom action bar live? Above home indicator,
     full width? Or floating button?
   - Paste a brief structural overview (header, tabs, sections, sticky
     elements that already exist)

2. API service layer
   - Read artifacts/mobile/services/api.ts
   - Show me the existing pattern for an authenticated POST request
   - Show me how response errors are handled (ApiError class etc)
   - Confirm: should I add new functions in services/timesheets.ts (new
     file) or extend an existing service file?

3. Existing user/auth state
   - Where does the app store the current user? Context? Zustand?
   - How does it know when the user is logged out (for nav purposes)?
   - We'll need to know the current user for filtering active entries

4. Toast / notification pattern
   - Confirmed in earlier diagnose: ToastContext at contexts/ToastContext.tsx
   - Show me the showToast signature

5. Active entry state — global or per-screen?
   - Clock-in button needs to know if user is currently clocked in to
     ANY project, not just the one viewed
   - If user is clocked into Project A and opens Project B, B's screen
     should show "Clock out of Project A first" not a green clock-in
     button
   - Best pattern? Zustand store? React Query polling /api/timesheets
     /active on mount + every N seconds? Context with manual refresh
     on focus?
   - What does the codebase already do for similar global state?

REPORT BACK before writing code. Just the 5 answers, then stop.

Do not modify any files yet. Do not push to git.