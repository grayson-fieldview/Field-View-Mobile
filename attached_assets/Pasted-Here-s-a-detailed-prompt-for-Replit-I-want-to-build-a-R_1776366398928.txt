Here's a detailed prompt for Replit:

I want to build a React Native mobile app version of this Field View web app for iOS and Android (to be submitted to the App Store and Google Play Store). The mobile app should share the same backend as the current web app — same AWS RDS database, same AWS S3 photo storage, same Stripe integration, and same authentication system.
Please do the following:

Analyze the current web app codebase and identify what can be reused in the mobile version (API calls, business logic, types, auth flow, state management, utility functions).
Set up a new React Native project using Expo in a new folder within this workspace (so it doesn't interfere with the existing web app code). Use Expo SDK latest version, TypeScript, and React Navigation for routing.
Replicate the core features from web to mobile, prioritized in this order:

Authentication (login, signup, password reset) — using the same backend auth endpoints
Photo capture with burst/rapid-fire mode (this is non-negotiable and the primary use case)
Photo upload to the existing S3 bucket via the existing API endpoints
Project list and project detail views
Task management
Checklists
Client sharing


Use the same API endpoints the web app already uses. Do not create a separate backend or duplicate API logic. The mobile app should hit the exact same Express routes.
Share types and interfaces between web and mobile where possible (consider a shared package structure).
For the photo capture feature, use expo-camera and implement burst mode that can take 5+ photos per second with metadata capture (GPS location, timestamp, project association).
Provide setup instructions for how I would:

Test the app on my iPhone using Expo Go
Build the app for production submission to App Store and Play Store
Configure environment variables (API base URL, Stripe publishable key, etc.)


Flag any limitations of developing React Native in Replit versus on a local Mac environment, and what I would need to do locally regardless of where I develop.

Before writing any code, give me a summary of:

What shared code/logic you found in the web app
The proposed folder structure for the mobile app
Which features you'll build first
Any concerns about building React Native in the Replit environment

Wait for my approval of the plan before writing code.