Remove the non-functional Google sign-in button from the mobile app's login screen. Per a previous audit, the button currently shows an "Available soon — please use your email and password" alert when tapped and does not trigger any actual OAuth flow. Removing it simplifies the UI and eliminates ambiguity about third-party authentication. A real Google + Apple sign-in implementation will be added in v1.1 post-launch.
File: artifacts/mobile/app/(auth)/login.tsx
Remove the following:

The <GoogleButton onPress={...}> component invocation around lines 72-79 (the rendered button on the login screen, including its onPress Alert.alert handler)
The function GoogleButton({ onPress }) component definition around lines 205-227
The function GoogleG() icon component around lines 229-255 (the monochrome "G" SVG used inside GoogleButton)
The googleBtn and googleText style entries in the StyleSheet (around lines 334 and 344)
Any "OR" divider, separator line, or "or sign in with" text that sat between the email/password form and the Google button — these only made visual sense with two auth options. If no such divider exists, skip this step.

Do not:

Add any replacement text, button, or messaging
Modify the email/password sign-in flow in any way
Modify the "Forgot password?" link or any other element
Touch any imports unless they become completely unused after removal (e.g., if Alert is only used by the removed Google button onPress, the Alert import can go too — but verify it's not used elsewhere first)

After applying, the login screen should contain only:

Field View logo / header
Email field
Password field (with show/hide toggle)
Sign In button
Forgot password link

Nothing else. No Google button, no signup link (already removed in previous edit), no third-party auth options.
Verification after applying:

TypeScript compiles cleanly
Grep artifacts/mobile/app/(auth)/login.tsx for Google, googleBtn, googleText, GoogleG — should return zero matches
Restart Expo workflow and confirm login screen renders correctly with no broken references

Show me the full diff before applying.