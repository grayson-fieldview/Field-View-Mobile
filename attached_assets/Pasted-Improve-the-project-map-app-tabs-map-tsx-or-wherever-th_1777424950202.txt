Improve the project map (app/(tabs)/map.tsx or wherever the project map lives) to show project name labels as rounded bubbles when zoomed in, while preventing visual overlap.
Behavior spec:

Wide zoom (>3 mile radius): show standard orange pin markers (current behavior). Tapping a pin opens the project (existing behavior).
Close zoom (≤3 mile radius): show project name as a rounded bubble label INSTEAD of a pin. The bubble has a small triangle/tail at the bottom pointing to the precise location. Tapping the bubble opens the project (same as tapping a pin currently does).
Overlap prevention: Even at close zoom, if two project bubbles would visually overlap on screen, BOTH revert to pin form. Only render as bubbles when there's room. The user can zoom further in to separate them.

Visual design for the bubble:

White background, dark text, subtle shadow
Tight padding (~6px vertical, ~10px horizontal)
Rounded corners (~8-10px radius)
Project name in ~13-14px Inter font, single line, truncate with ellipsis if name exceeds ~150px width
Small triangle pointer at the bottom-center, ~8px wide, pointing to the project's exact lat/lng
When the bubble represents a project the user has recently interacted with (selected state), add a subtle brand orange (#F09001) border or accent

Implementation notes:

Find the existing map screen — likely uses react-native-maps. Show me which library and current marker implementation.
Determine zoom level. Listen to onRegionChange and compute the visible region's mile radius from the deltas. Threshold for switching pin↔bubble is 3 miles.
Overlap detection. Two reasonable approaches — pick whichever fits the existing code best:

Pixel-based: project each project's lat/lng to screen coordinates using the map view's transform, calculate each bubble's screen rect (width = measured text width + padding), check pairwise rect intersection
Distance-based shortcut: at the current zoom, calculate the geographic distance threshold below which bubbles would visually overlap (depends on map width and average bubble pixel width). Two projects closer than this threshold both render as pins.

Distance-based is simpler and 90% accurate; pixel-based is precise but more code. Use distance-based unless you find the map library exposes a clean lat/lng → screen pixel API.
Build the custom marker. react-native-maps supports custom marker views via the <Marker> children — render a <View> with the bubble styling instead of relying on the default pin. The triangle tail uses absolute positioning + transparent borders trick (standard CSS-in-RN pattern).
Performance. If there are many projects, recompute overlap state only when the map region changes, not on every render. Memoize the bubble-vs-pin decision.
Tap behavior. Both bubble and pin variants should call the same existing onPress handler that opens the project.

What NOT to do:

Don't change the map's wide-zoom behavior or the pin styling
Don't add clustering (separate feature, not in scope)
Don't add filters, search, or any other map UX changes
Don't change the orange action button at the top right of the map

Verification after applying:

TypeScript compiles cleanly
Restart Expo, confirm clean Metro bundle
On device:

Wide zoom over Florida → see orange pins
Zoom into West Palm Beach → pins should turn into bubbles with project names below ~3 miles
If two projects are very close, they stay as pins until you zoom in further to separate them
Tapping any bubble or pin opens the project as before



Show me the design + implementation plan first (which file, library used, overlap approach chosen). Then show the diff. Then apply.