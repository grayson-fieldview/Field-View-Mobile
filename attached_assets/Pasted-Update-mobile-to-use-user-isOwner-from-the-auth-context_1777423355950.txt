Update mobile to use user.isOwner from the auth context instead of fetching GET /api/account for owner detection. The web backend now exposes isOwner: boolean on the /api/auth/user response (deployed today).
Changes:

Add isOwner to user types

In services/api.ts, find the BackendUser interface (or wherever the user shape is defined). Add isOwner?: boolean (optional for backward compatibility — accounts on older code paths might not have it set yet, default to false).
Also check services/mappers.ts — if there's a mapBackendUser function, ensure isOwner is passed through to the local User type.

Add isOwner to AuthUser type in AuthContext

Wherever the AuthContext defines its user shape (likely contexts/AuthContext.tsx), add isOwner: boolean and ensure it's populated from the backend response.

Update Settings/Profile screen owner detection

In app/(tabs)/profile.tsx (or wherever the Danger Zone owner detection lives):

Remove the useEffect that fetches /api/account
Remove the local isOwner state and the loading state
Replace with: const isOwner = user?.isOwner ?? false;
Render the appropriate button based on this value (no loading state needed since user is already available from auth context)


Remove getAccount() from api.ts

In services/api.ts, remove the getAccount method that was added for owner detection. It's no longer used. If you want to keep it for future use, leave it — but no consumers exist now.

Remove diagnostic logging

Remove all the [owner-detect] and [api] /api/account logging that was added for debugging. Specifically:

In app/(tabs)/profile.tsx: remove the [owner-detect] console.log lines (start, response, FAIL)
In services/api.ts: remove the [api] !ok body logging AND the /api/account 200 body logging

Keep the existing [api] → and [api] ← logs, those are pre-existing instrumentation.

Verify


TypeScript compiles cleanly
Restart Expo, confirm clean Metro bundle
On device: sign in as the owner, navigate to Settings, should see "Delete account" button (NOT "Leave team")
"Unauthorized" toast should NOT appear anymore (since we're not calling the failing endpoint)

Show me the diff before applying.