Diff 1 approved with one small addition.

Add projectName: string to ClockInReceiptData interface. Threading it through fireClockInReceipt's signature so callers pass it. Reasoning: if a project gets archived/deleted server-side between the clock-in fire and the user tapping the notification (rare but possible), the project detail screen needs a fallback display string. Including projectName in the payload preserves the snapshot at clock-in time.

Updated interface:
export interface ClockInReceiptData {
  type: "clock_in_receipt";
  projectId: number;
  projectName: string;          // NEW — snapshot at clock-in time
  entryId: string;
  clockInTime: string;
}

Updated function signature:
fireClockInReceipt(projectName: string, projectId: number, entryId: string, clockInTime: Date): Promise<void>
(unchanged — projectName was already the first param, now ALSO threaded into the payload data)

Apply that small change, then proceed with Diff 2 (services/geofencing.ts strip + silent-auto sequence).

For Diff 2:
- Remove all queue/dedupe/emitter infrastructure (~70 lines per your diagnosis count)
- Keep recordClockInForRegion(projectId) — still needed
- Replace the previous enqueueClockInPrompt(...) call with silent-auto sequence in this exact order:
  1. recordClockInForRegion(regionId) — stamp BEFORE async work for retry-storm protection
  2. try { const entry = await api.clockIn(projectId, undefined, "auto_geofence") }
  3. await notifications.fireClockInReceipt(project.name, projectId, entry.id, new Date(entry.clockIn))
  4. catch (err): if 401, log [geofence] auth failed: session expired and stop; else log [geofence] silent-auto failed: <err.message>. No retry, no notification, debounce stamp stays
- Add code comment explaining the stamp-before-API ordering choice (failure mode: 5-min retry gap; protects against GPS-jitter retry storms during backend outages)
- Keep the [geofence] enter accepted: ... log line for diagnostic continuity
- Keep all existing filter chain logs and behaviors
- The runGeofenceTaskBody signature does NOT change — bypassFilters opts param still works

Standing by.