Three answers, plus one caveat. Approved to proceed after acknowledging.

1. Endpoint location: option (a). The production backend mobile hits at app.field-view.com is the monolithic server/routes.ts in the Web workspace (grayson-fieldview/Field-View, deployed via Vercel) — same repo where Session 27.6 CSRF middleware and yesterday's projectId coerce fix went. The artifacts/api-server stub in this monorepo is unrelated and not where the endpoint goes.

I'm writing the Web Agent prompt for the geofence-eligible endpoint separately. It'll land in server/routes.ts in the Web workspace and ship to prod via the standard Vercel push flow. Production base URL unchanged: https://app.field-view.com.

End-to-end test of the new endpoint requires prod deploy of it (since the Dev Build's EXPO_PUBLIC_API_BASE_URL is hardcoded to app.field-view.com). Session 30 mobile work can land first since it doesn't actually consume the endpoint's data until Session 31's enter handler. Acceptance test from your spec ("console logs N regions registered") will pass once the endpoint is live in prod.

Write mobile code against the contract as documented:
- GET /api/projects/geofence-eligible
- Returns { id: number, name: string, latitude: number, longitude: number, lastActivityAt: string }[]
- Already filtered + sorted + capped server-side; mobile trusts the response

2. expo-task-manager install approved. pnpm --filter @workspace/mobile add expo-task-manager, pin ~14.0.x.

CAVEAT — the current iPhone Dev Build doesn't have this native module yet (compiled before this install). Until the EAS rebuild in Session 34, simply importing or referencing expo-task-manager at module load time will throw on-device. Make sure:
- Geofencing service is imported lazily where possible (dynamic import, or import inside a function body), OR
- All TaskManager.defineTask + Location.startGeofencingAsync calls are gated behind a runtime check (Platform.OS === "ios" && status === "always-granted") so they never execute on a Dev Build that lacks the native binding
- Confirm in your diagnosis after install that simply running the app doesn't crash, even before any geofence registration is triggered

This isn't optional — if the Dev Build crashes on launch because of a missing native module, you've broken everything we shipped in Session 29 too. Verify on dev build before claiming session-complete.

3. lastActivityAt as string (ISO 8601) — approved. Opaque sort key, no on-device parsing.

Proceed: install expo-task-manager → diagnosis confirmation that app still launches → services/geofencing.ts → hooks/useGeofenceSync.ts → (tabs) layout integration → debug surface. Show me each diff. Don't push to git.