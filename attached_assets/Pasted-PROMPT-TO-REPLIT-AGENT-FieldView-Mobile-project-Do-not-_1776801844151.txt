PROMPT TO REPLIT AGENT (FieldView Mobile project):
Do not write or change any code. I need a diagnostic report only. Read through the codebase and answer the following questions precisely. If you can't find something, say "not found" — do not guess.
1. API / Backend configuration

Is there an environment file (.env, app.config.js, expo.json, .env.local, etc.)? List every env var name defined in it (values redacted is fine).
Search the codebase for any of: app.field-view.com, field-view.com, localhost, API_BASE, BASE_URL, API_URL, apiUrl, baseURL. Report every file and line where these appear, and what value is assigned.
Is there any direct database connection code? Search for: pg, Pool, postgres, drizzle, prisma, DATABASE_URL, RDS. Report every result with file path and a 3-line snippet.
Is there any direct AWS SDK usage? Search for: aws-sdk, @aws-sdk, S3Client, PutObjectCommand, AWS_ACCESS_KEY. Report every result with file path.

2. Data fetching layer

What library is used for API calls? (fetch, axios, React Query, SWR, Apollo, tRPC, etc.)
Find the function or hook that fetches the list of projects. Paste the full function code and the file path.
Find the function or hook that fetches photos for a project. Paste the full function code and the file path.
Find the function that uploads a photo. Paste the full function code and the file path.

3. Auth

How does the mobile app authenticate users? (session cookie, JWT in header, Firebase, Supabase, Passport, custom, etc.)
Find the login function. Paste it and its file path.
Where is the auth token/session stored? (AsyncStorage, SecureStore, memory, cookie)
Is there code to send the auth token with each API request? Paste the relevant interceptor / header-attach code.

4. Real-time sync

Search for: WebSocket, socket.io, Pusher, Ably, EventSource, SSE, subscribe, onSnapshot, realtime. Report every result with file path.
Is there any polling or refetch logic? (React Query refetchInterval, setInterval on queries, AppState listeners for foreground refresh)
Is there pull-to-refresh implemented anywhere? Report file paths.

5. Structure

Paste the contents of package.json (just the dependencies and devDependencies sections).
List the top-level folders inside the main source directory (src/, app/, whatever is used).

Return all findings as a single structured report. No code changes. No fixes. Just tell me what's there.