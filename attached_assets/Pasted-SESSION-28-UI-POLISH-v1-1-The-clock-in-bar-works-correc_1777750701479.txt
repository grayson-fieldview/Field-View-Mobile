SESSION 28 — UI POLISH (v1.1)

The clock-in bar works correctly but visually competes with the
primary feature (photos/Take Photo). Two changes to subordinate it.

CHANGE 1 — Auto-hide on scroll, State 1 only
The sticky bar in State 1 (Clock In available) should:
- Be visible at the top of the scroll position
- Slide down off-screen smoothly when user scrolls DOWN
- Slide back up into view when user scrolls UP
- State 2 (clocked in to current project) — STAYS visible always
- State 3 (clocked in to other project) — STAYS visible always

Implementation:
- Use the existing ScrollView's onScroll handler
- Track scroll direction by comparing current contentOffset.y to
  the previous value (use a useRef, not state, to avoid re-render
  spam)
- Animate translateY using Animated.Value or react-native-reanimated
  (whichever the codebase already uses for similar effects — check
  imports in app/_layout.tsx and app/(tabs)/* first)
- Threshold: don't react to scrolls smaller than ~10pt to avoid
  twitching from finger micro-movements
- Animation: ~200-250ms, spring or timing
- When state changes from 1 → 2 or 1 → 3, force the bar back to
  visible regardless of scroll position

CHANGE 2 — Quieter State 1 visual
Current: full orange fill with white text. Too dominant.

New State 1:
- Background: white (or very pale neutral, match the app's card bg)
- Top border: 1px hairline in neutral-200ish
- Inside: clock icon + "Clock In" text, both in brand orange
- No filled button — the whole bar acts as the tap target
- Same height (≥56pt minimum for tap target)
- Same padding pattern with safe-area inset

States 2 and 3: leave unchanged for now.

DIAGNOSE FIRST
1. Which animation library does the codebase use? Reanimated?
   Animated? Plain useState? Show me one example from elsewhere
   in the codebase that does scroll-triggered hiding (FloatingTabBar
   may already do this).
2. The ScrollView in app/project/[id].tsx — does it already have
   an onScroll handler I'd be hooking into? If yes, paste the
   current handler so the new logic composes cleanly.
3. Confirm State 2 and State 3 stay as-is (no color or layout
   changes).

REPORT before implementing.

DELIVERABLE
1. Files touched
2. Animation library used and why
3. Confirmation State 2 and State 3 are untouched
4. Wait for Grayson to test on iPhone before declaring done

Do not push to git.