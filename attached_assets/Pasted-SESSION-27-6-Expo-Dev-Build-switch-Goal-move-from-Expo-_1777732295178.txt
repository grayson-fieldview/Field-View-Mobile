SESSION 27.6 — Expo Dev Build switch.

Goal: move from Expo Go to a Dev Build (with expo-dev-client) so we
can use background location, expo-task-manager, and other native
modules in subsequent sessions.

This is the biggest pre-req session. Once it's done, all later
mobile work is unblocked.

CONTEXT
- Apple Developer Program is active
- Confirmed via Session 27.5 backend pre-flight: API is mobile-ready
- Currently running on Expo Go (no eas.json, no ios/, no android/)
- Will use EAS Build (cloud) — no local Mac build environment

DIAGNOSE FIRST — confirm prerequisites before any installs

1. Read package.json — list current Expo SDK + dev dependencies.
   Specifically:
   - expo, react-native, expo-router versions
   - Any existing eas-cli or @expo/cli references
   - Confirm no expo-dev-client is installed yet

2. Read app.json — paste it in full. Specifically:
   - ios.bundleIdentifier
   - ios.infoPlist (current usage descriptions)
   - android.package
   - plugins array
   - expo.scheme (does the app have a custom URL scheme already?)

3. Check for any EAS-related state:
   - Does .easignore exist?
   - Is there an .easproject hidden directory?
   - Any references to projectId in app.json?

4. Confirm npm/eas-cli availability:
   - Run: which eas (may not be installed globally)
   - Run: npx eas --version (should work via npx without install)
   - DO NOT install eas-cli globally yet — we'll decide

REPORT BACK with all 4 outputs before any changes. I want to confirm
state before installs.

IMPLEMENTATION (after I approve)

Step 1: Install expo-dev-client
   npx expo install expo-dev-client
   Confirm package.json updated, package-lock.json updated.

Step 2: Initialize EAS project
   npx eas init
   This will prompt for Expo account login and create a projectId
   in app.json under expo.extra.eas.projectId. Walk through the prompts.
   STOP and report back with the projectId once written to app.json.

Step 3: Create eas.json with two profiles
   - "development" profile: distribution: internal, developmentClient: true,
     ios.simulator: false (we want physical device builds)
   - "preview" profile: distribution: internal, ios.simulator: false
   - "production" profile: skeleton, won't use yet
   
   Use the standard Expo template format. Paste the full eas.json
   contents before proceeding.

Step 4: STOP HERE. Do not run eas build yet.
   The first build requires:
   - Apple Developer credentials configured in EAS
   - Bundle identifier registration with Apple
   - Push notification certificate (optional but worth doing now)
   
   These steps require the human's Apple credentials + interactive
   prompts. Surface a checklist of what the human needs to provide
   before we run the first build.

DELIVERABLE
1. All 4 diagnose outputs
2. expo-dev-client installation confirmation (package.json + lock diff)
3. EAS project init result (projectId + any other state added)
4. eas.json contents
5. Pre-build checklist for the human (Apple credentials, bundle ID
   verification, etc.)

Do not run eas build. Do not push to git. Do not modify replit.md yet.