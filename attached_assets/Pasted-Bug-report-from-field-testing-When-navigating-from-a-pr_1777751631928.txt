Bug report from field testing:

When navigating from a project detail screen back to the Projects list:
- Sometimes the list shows the loading spinner and requires pull-to-refresh
  to populate
- Sometimes the list shows ONLY the project we just came from, not all
  projects
- Both states are wrong — should immediately show the full projects list
  as it was before navigating in

DIAGNOSE ONLY — DO NOT WRITE CODE YET.

1. Find the Projects list screen — likely artifacts/mobile/app/(tabs)/index.tsx
   or app/projects/index.tsx. Identify the file path and paste the
   data-fetching pattern:
   - What's providing the projects data? DataContext? React Query?
     Direct fetch?
   - What's the cache/state key for the projects list?
   - Is there pagination, infinite scroll, or filter state in query
     params?
   - Is there a useFocusEffect, useEffect, or similar that triggers on
     navigation back to this screen?

2. Find the project detail screen at app/project/[id].tsx (we worked on
   this in Session 28). Does it:
   - Mutate any cache or state that the projects list depends on?
   - Pass any state via navigation params (router.back() with state,
     nav.setParams, etc.) that affects the list view?
   - Set any "active project" or "current project" state in a Context
     or store that gets carried back to the list?

3. Check the search/filter state on the Projects list. Is there a
   search input, filter, or sort state that gets persisted across
   navigation? Could the "single project showing" symptom be a search
   filter accidentally persisted?

4. Check DataContext (artifacts/mobile/contexts/DataContext.tsx). Per
   Session 28 diagnose, it does its own throttled fetch + AppState
   foreground refresh + per-screen-focus refresh. Specifically:
   - What triggers a projects refresh? Mount? Focus? Foreground?
   - Is there a "loading" state that replaces the existing list while
     refreshing, or does it merge new data into existing data?
   - When the focus listener fires on returning to the projects list,
     is it possibly clearing the projects array first and then refetching,
     causing the spinner flash?

5. Check the Tab/Stack nav setup. Is the Projects list a tab screen or
   a stack screen? When user navigates to project/[id].tsx and back,
   does the list screen unmount or just lose focus?

REPORT BACK with all 5 findings before proposing a fix. There are
several plausible causes. Most likely culprits in order:
(a) DataContext clears projects state during refetch instead of stale-while-revalidate
(b) Search filter state accidentally persisted
(c) useFocusEffect refetch racing with cached state

After diagnose, propose smallest possible fix and we'll discuss.

Do not push to git. Do not modify code or config.