Diagnose approved. Implement the proposed fix with two refinements.

REFINEMENT 1 — Skip the belt-and-suspenders refresh
Do NOT add refresh({ force: true }) to the list's useFocusEffect.
The 4s throttle is correct as-is once the upstream wipe stops. We
want regressions to be visible, not masked by aggressive refreshing.

REFINEMENT 2 — Recover existing corrupted caches
Field testers (and Grayson on his iPhone right now) may have a
corrupted AsyncStorage from prior project taps. The upserted-array
fix won't unbreak existing corrupted state until a forced refresh
runs.

Add a one-time recovery: on app mount in DataContext, if the
hydrated projects array from AsyncStorage has fewer than N items
(propose threshold based on what's reasonable — maybe 0 or 1?), kick
off a force: true refresh once.

Actually wait — this could mask legit "new user with empty cache"
states. Let me reconsider.

BETTER REFINEMENT 2:
Instead of a heuristic, just bump the local storage version key. If
DataContext stores projects under something like @fieldview/projects/v1,
bump to v2. On hydrate, the v2 key returns null, the cache loads empty,
and the next refresh (the auth-ready force: true one at line 245)
populates correctly from the server. Existing v1 data becomes orphaned
but harmless (we can leave it or add cleanup later).

Look for the AsyncStorage key DataContext uses. If versioning isn't
already a pattern, propose a small AsyncStorage.removeItem on the
old key + write to a new key approach. Either way:

Goal: any user who launches the app after this deploy gets a clean
projects array, not whatever corrupted state they previously had.

IMPLEMENTATION

1. Fix loadProjectDetail in DataContext.tsx as proposed:
   const upserted = projectsRef.current.some((p) => p.id === mappedProject.id)
     ? projectsRef.current.map((p) => (p.id === mappedProject.id ? mappedProject : p))
     : [mappedProject, ...projectsRef.current];
   await persistProjects(upserted);

2. Add the storage version bump (or equivalent recovery) per
   refinement 2 above.

3. Do NOT touch mergeById itself, doSync, or the focus refresh logic.
   Other call sites of mergeById are correct.

4. Add a code comment above the loadProjectDetail upsert explaining
   what was wrong and why the upsert is correct, so the next reader
   doesn't reintroduce the bug.

VERIFICATION

After fix, on Grayson's iPhone (hot reload):
1. Navigate into Janet Wexler from projects list
2. Hit back button → projects list shows ALL projects (Janet, Brad &
   Lauren, Rebecca, Tom, etc.), no spinner needed
3. Tap into Brad & Lauren, hit back → still all projects, no degradation
4. Force-quit app, reopen → still all projects, no spinner

DELIVERABLE
1. Diff for DataContext.tsx (the upsert fix + recovery refinement)
2. AsyncStorage key strategy (version bump vs cleanup vs other)
3. Brief explanation of the code comment added
4. Confirmation mergeById and doSync are untouched

Wait for Grayson's iPhone test pass before declaring done.

Do not push to git.