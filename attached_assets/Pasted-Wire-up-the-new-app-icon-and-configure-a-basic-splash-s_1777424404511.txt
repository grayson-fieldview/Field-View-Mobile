Wire up the new app icon and configure a basic splash screen for production builds. The master icon has just been replaced at artifacts/mobile/assets/images/icon.png (1024×1024 PNG, no transparency, no rounded corners — Apple/Google add corners automatically).
Investigation step first:

Open app.json (or app.config.js, whichever exists) in artifacts/mobile/.
Look at the current values for:

expo.icon
expo.ios.icon
expo.android.icon
expo.android.adaptiveIcon
expo.splash
expo.ios.bundleIdentifier (should be com.fieldview.app from the previous prompt)
expo.android.package
expo.ios.infoPlist.NSCameraUsageDescription etc. (should already be set)


Show me the current state of those values before making changes.

Changes to apply:

expo.icon → "./assets/images/icon.png" (path is relative to the project root, which is artifacts/mobile/)
expo.ios.icon → "./assets/images/icon.png" (same master icon — Apple uses this for all iOS contexts)
expo.android.icon → "./assets/images/icon.png" (fallback for older Android versions)
expo.android.adaptiveIcon — set up properly:
json"adaptiveIcon": {
  "foregroundImage": "./assets/images/icon.png",
  "backgroundColor": "#F37021"
}
Note: Android adaptive icons use a separated foreground + background. Using the full icon as the foreground will work but won't be ideal — the foreground image is supposed to be just the logo on a transparent background, so the system can apply different shapes (circle, squircle, square) per device. For now, this is acceptable for v1; we can refine in a later iteration with a proper transparent foreground.
expo.splash — configure with brand orange:
json"splash": {
  "image": "./assets/images/icon.png",
  "resizeMode": "contain",
  "backgroundColor": "#F37021"
}
Using the full icon as the splash for v1. Future iteration: a dedicated splash image with just the logo (no background) on a transparent canvas, using the orange backgroundColor to fill.
Verify the orange hex is correct. The icon's orange might be slightly different from #F37021 (Field View brand orange from past sessions). If you can read the icon file's pixel color, do so. Otherwise use #F37021 and we'll adjust if it looks off.

Do not:

Touch any other app.json fields (bundle ID, version, permission strings, etc.)
Modify image files
Add any new image assets
Run expo prebuild or eas build — those are separate steps for later

Verification after applying:

The four icon paths are set correctly
Splash configuration is set with the orange background
Restart Expo workflow, confirm clean Metro bundle (Expo will validate the asset paths and complain if any are missing)
On device: when the app starts up in Expo Go, you should see the orange splash with the icon centered before the app loads

Show me the current app.json state first, then the proposed diff. Then apply.