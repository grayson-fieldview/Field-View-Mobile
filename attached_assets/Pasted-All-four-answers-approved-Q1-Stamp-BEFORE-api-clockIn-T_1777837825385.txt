All four answers approved.

Q1: Stamp BEFORE api.clockIn. The "5-min retry gap on failure" cost is acceptable; protecting the backend from GPS-jitter retry storms during outages is worth more. Code comment should explain this for future maintainers.

Q2: Notification copy:
- Title: "Clocked in"
- Body: "[Project Name] · 3:15 PM"
Use middle dot (·) U+00B7. Format the time in the user's locale (toLocaleTimeString) — most contractors are in US so AM/PM with no seconds. Strip leading zeros for hours: "3:15 PM" not "03:15 PM".

Q3: DELETE the entry, NOT clock-out. Cleaner data; "Undo" should mean reverse, not nullify.

Requires new backend endpoint: DELETE /api/timesheets/:id with safety constraints:
- User can only delete entries they own (userId match)
- Entry must have source='auto_geofence' (manual entries can't be undone via this path)
- Entry must be created within last 60 minutes (narrow undo window)
- Returns 204 on success, 403 on ownership/source mismatch, 404 on not found, 410 on outside-window

I'll send Web Agent the parallel prompt for this when you start implementation. Mobile-side: api.deleteTimeEntry(entryId): Promise<void> in services/api.ts, called from the Undo banner.

Q4: Add profile-screen row for notification settings deep link. Small UX addition. Render conditionally based on current notification permission status — don't show the row if permission is "granted":
- Permission granted: hide row entirely (no clutter)
- Permission denied or undetermined: show row "Receipt notifications: Off" with tap → Linking.openSettings() (iOS deep link to app's settings page)
- Place under existing Permission row in Geofence Debug section, OR somewhere in main profile area — your call, lean toward keeping it visible during early product life so contractors notice it and can self-correct

PROCEED with implementation. Same constraints as before:
- Show diffs sequentially (notifications.ts, geofencing.ts, layout cleanup, deep-link handler, project detail banner, profile row, app.json)
- TypeScript typecheck must pass before claiming "diff complete"
- Restart Metro before asserting file is clean if anything looks off
- Do NOT push to git — I push from Mac/Replit Git pane

Begin with diff 1 (services/notifications.ts as new file). Standing by.