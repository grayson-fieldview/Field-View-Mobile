Final review checklist before push. Please run these spot-checks and report results:

1. The B4 source check in handleGeofenceExit:
   grep -n "active.source" services/geofencing.ts
   
   Should show the comparison against "auto_geofence" string literal. Confirm exact form 
   and that it fails-closed (rejects on null/undefined/manual/edited).

2. The Enter-side cancel hook's race invariant comment from Diff 3 review:
   grep -n "SAFE\|idempotent\|redundant server roundtrip" services/geofencing.ts
   
   Should show the inline comment we added per "race invariant on cancel + re-exit."

3. The hydration ordering microtask warning from Diff 5a:
   grep -n "microtask\|fragile\|mirroring effect" contexts/TimesheetContext.tsx
   
   Should show the inline doc near activeRef declaration.

4. The B1 ordering comment from Diff 3 review:
   grep -n "B1 last\|cheapest\|free local" services/geofencing.ts
   
   Should show the explanation comment.

5. ClockInReceiptBanner has no remaining references in any code file:
   grep -rn "ClockInReceiptBanner" --include="*.ts" --include="*.tsx" .
   
   Should return zero hits (rename was clean).

6. The cold-start hydration path:
   grep -n "LAST_SEEN_KEY\|last_seen_active_v1" contexts/TimesheetContext.tsx
   
   Should show the persistence write on session-id transitions and the IIFE on initial mount.

7. Strip attached_assets/Pasted-*.txt from staging before I push:
   Suggest one of: 
   (a) Run `git rm --cached attached_assets/Pasted-*.txt` so they're untracked but local
   (b) Add `attached_assets/` to .gitignore (project-wide; affects future paste files too)
   (c) Just don't stage them when I commit
   
   My preference is (b) — add `attached_assets/` to .gitignore. These files are review 
   artifacts that should not be in version control. If you've already added .gitignore for 
   them, confirm. Otherwise add it as a separate uncommitted change I can stage with the 
   real diff.

8. Run pnpm typecheck once more from clean state. Report:
   - The 2 pre-existing errors (capture.tsx CameraMode, FloatingTabBar bottom-tabs)
   - Zero new errors from S32a-mobile
   - Total error count

Report all 8 results. Once clean, I push from the Git pane.