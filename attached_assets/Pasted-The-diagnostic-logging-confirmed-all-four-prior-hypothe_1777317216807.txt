The diagnostic logging confirmed all four prior hypotheses are wrong. The console output shows:

[photos] received from backend: 75
[photos-debug] route id: 8 string
[photos-debug] mappedProject.id / idStr: 8 8
[photos-debug] first photo projectId: 8 string
[photos-debug] match check (=== route id): true
[photos-debug] match check (=== idStr): true
[photos] total after persist: 75

Photos are loaded correctly, mapped correctly, persisted correctly, and the projectId on the photos matches the route id exactly. Yet the grid renders empty.
The bug is in the render layer of the project detail screen, not the data layer. Investigate:

Re-read app/project/[id].tsx from top to bottom. Find:

Where photos is consumed — is it from useData() / DataContext, or from local state, or from a different context?
The exact filter expression that produces projectPhotos — what fields does it compare?
Whether projectPhotos is the same variable used to render the grid, or whether the grid reads a different array
Any useMemo / useCallback dependencies that might be stale


Find the actual JSX that renders the photo grid. Show me:

The component or markup that renders each photo
Any conditional rendering wrapping it (e.g., {condition && <Grid />} or {loading ? ... : ...})
The data array being mapped to render photos
Whether there's a separate "no photos" empty state being shown — and what condition triggers it


Check for hidden filters:

Search for any filter on the photos array beyond projectId === id
Look for filters by archived, deleted, visible, userId, uploaded, remote, or any boolean flag
Look for date range filters
Look for filters based on user role / accessLevel


Add temporary logging to the project detail screen itself (not DataContext this time):

Log the length of the photos array consumed from context: console.log('[render-debug] photos from context:', photos.length)
Log the length of projectPhotos after filtering: console.log('[render-debug] projectPhotos after filter:', projectPhotos.length)
Log the first projectPhoto: console.log('[render-debug] first projectPhoto:', JSON.stringify(projectPhotos[0]))
Log immediately before the grid renders: console.log('[render-debug] about to render grid, count:', projectPhotos.length)
Log inside any conditional that might hide the grid: console.log('[render-debug] grid condition:', <whatever the condition is>)


Show me the relevant code sections (filter logic + render block + any conditional gates) before adding the logging, so I can sanity-check the suspects.

Show me the code first, then the diff for the new logging. Do not change business logic.