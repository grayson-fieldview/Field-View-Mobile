Diff 2 approved as-is. All decisions are correct:
- Status as string, not union — right tradeoff
- No exported types — right premature-surface call
- autoUndoTimeEntry docstring extension — preempts Diff 5 confusion
- allowEmptyBody true — defensive
- Idempotency invariant cross-referenced

For Diff 3 — one edge case worth designing around upfront:

When the Enter-side cancel-pending-exit hook calls findPendingExitsForRegion, the returned 
record may have pendingExitId === null (unsent retry state — exit-detected POST failed 
previously, mobile is waiting to retry on next foreground). In that case:
- We can't call geofenceExitCancelled (no ID to send)
- But we should still remove the local record (it's stale; user re-entered before retry)
- The server has nothing to cancel because nothing was ever scheduled

Branch in the Enter-side cancel hook:
  if (record.pendingExitId !== null) {
    await api.geofenceExitCancelled(record.pendingExitId);
  }
  await removePendingExitById(record.pendingExitId ?? <fallback>);

The fallback for removal-by-ID when pendingExitId is null is awkward. May be cleaner to expose
a removePendingExitByCompoundKey(timeEntryId, regionId) variant in pendingExits.ts, OR just 
call removePendingExitByTimeEntryId since timeEntryId is always present. Up to your judgment.

Also: if findPendingExitsForRegion ever returns >1 result, log warning to Sentry. Server's 
partial unique index should make this impossible — >1 = corruption signal.

Proceed to Diff 3. Per standing instruction, STOP and request explicit approval before Diff 4. 
Show me the full geofencing.ts diff with attention to:
- L246 guard removal + Enter/Exit branch routing
- handleGeofenceExit's 5-stage filter chain (especially the B4 source check for manual sessions)
- Enter-side cancel-pending-exit hook (with the null-pendingExitId edge case above)
- triggerSyntheticExitForTesting(regionId)
- Whether you kept the watchdog setTimeout from spec Part 2D or dropped it (foreground poll 
  is the truth source either way — if you dropped it, defensible)