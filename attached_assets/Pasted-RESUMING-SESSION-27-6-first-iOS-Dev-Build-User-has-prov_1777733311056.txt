RESUMING SESSION 27.6 — first iOS Dev Build.

User has provided all required credentials:
- EXPO_TOKEN secret (already used for eas init)
- EXPO_APPLE_ID secret (Apple Developer account email)
- EXPO_APPLE_APP_SPECIFIC_PASSWORD secret (xxxx-xxxx-xxxx-xxxx format)
- Apple Team ID: D92J7GZBAY
- Apple Team Name: Field View LLC

User is at their iPhone, ready to scan a QR code for device registration.

EXECUTE in this order:

STEP 1: Register Grayson's iPhone with the Apple team

  Run: EXPO_TOKEN=$EXPO_TOKEN pnpm --filter @workspace/mobile exec eas device:create
  
  This will:
  - Authenticate via EXPO_TOKEN
  - Prompt for Apple credentials — provide them via env vars:
    EXPO_APPLE_ID=$EXPO_APPLE_ID 
    EXPO_APPLE_APP_SPECIFIC_PASSWORD=$EXPO_APPLE_APP_SPECIFIC_PASSWORD
  - Generate a QR code in the terminal AND a URL
  - Wait for the device to register
  
  PASTE the QR code output / URL to chat as soon as it appears so the
  user can scan it. The QR code expires in ~10 minutes — don't dawdle
  showing it.
  
  Once Grayson scans and confirms registration on his iPhone, the
  command will exit. Show the success output (device name, UDID,
  team it was registered to).

STEP 2: First Dev Build for iOS

  Run:
  EXPO_TOKEN=$EXPO_TOKEN \
  EXPO_APPLE_ID=$EXPO_APPLE_ID \
  EXPO_APPLE_APP_SPECIFIC_PASSWORD=$EXPO_APPLE_APP_SPECIFIC_PASSWORD \
  pnpm --filter @workspace/mobile exec eas build --profile development --platform ios --non-interactive
  
  Expected behavior:
  - Authenticates via tokens (no interactive prompts)
  - Auto-registers bundle ID com.fieldview.app to the team if not yet
    registered (Grayson confirmed it's available)
  - Auto-generates Distribution Certificate
  - Auto-generates iOS Provisioning Profile (using the registered
    device from Step 1)
  - Submits build to EAS cloud
  - Returns a build URL (https://expo.dev/.../builds/...)
  - Polls build status; build takes 15-25 minutes typically
  
  PASTE the build URL to chat as soon as it appears. The user can
  watch progress at that URL while the build runs.
  
  When the build completes, EAS will print:
  - Build status (success / failed)
  - If success: an install URL (typically https://expo.dev/.../builds/<id>
    which displays a QR code that opens in Safari on iPhone to install)
  
  PASTE the install URL when ready.

STEP 3: Document the result, do NOT push to git

  - If the build succeeded: confirm in chat. The user will install on
    their iPhone, then we move to verifying hot-reload works in the
    dev build (separate step after they confirm install).
  - If the build failed: paste the relevant error log section. Common
    failure modes:
    - New Architecture compatibility issue with a native module
    - Missing or invalid Apple credentials
    - Bundle ID auto-registration failure (rare)
    - React Compiler babel plugin issue
  
  Do NOT push to git, do NOT modify replit.md.

KNOWN POTENTIAL ISSUES TO WATCH FOR

1. New Architecture (newArchEnabled: true is set in app.json). All
   installed native modules are reportedly New-Arch-ready, but if any
   fails the build, that's the likely cause.

2. React Compiler (experiments.reactCompiler: true). Could cause babel
   plugin issues during the build's Metro bundling step.

3. App-specific password "Apple ID" auth. Sometimes EAS prompts for an
   interactive 2FA code despite the env vars. If it does, surface the
   prompt in chat — Grayson can provide a 2FA code from his trusted
   device.

DELIVERABLE
1. Step 1 output: device registration QR/URL, then success confirmation
2. Step 2 output: build URL, build status, install URL (or error logs)
3. Total elapsed time
4. Anything unexpected

The agent's role here is mostly running commands and surfacing
output to chat. The user will be watching the EAS dashboard and
their iPhone in parallel.