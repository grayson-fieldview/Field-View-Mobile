I need to update the bottom tab bar navigation on the Field View mobile app. Currently, there's a floating orange camera button in the bottom-left corner that overlaps the tab bar, and it's rendering as an oval instead of a circle. I want to redesign this to match an iOS-style centered floating action button (FAB) pattern.
Required changes:

Remove the existing floating orange camera button from the bottom-left corner. It should no longer sit above the Projects tab.
Restructure the bottom tab bar to have 4 tabs with a centered FAB between them:

Left side: Projects tab, then Map tab
Center: Orange circular camera FAB (floating, elevated above the tab bar)
Right side: Tasks tab, then Profile tab
The Map tab should shift slightly left and the Tasks tab should shift slightly right to create visual space for the centered FAB. Distribute the four tabs evenly around the center point so the FAB sits cleanly in the middle without overlapping any tab label or icon.


Fix the FAB to be a perfect circle, not an oval:

Set explicit equal width and height (e.g., width: 64, height: 64 in React Native, or w-16 h-16 in Tailwind).
Set borderRadius: 32 (exactly half of width/height) — do NOT use a percentage-based border radius, and do NOT let flex or parent containers stretch it.
Wrap the button in a container with aspectRatio: 1 as a safeguard.
The oval issue is likely caused by either (a) the parent flex container stretching the button, (b) flex: 1 being applied to the button itself, or (c) using borderRadius: '50%' which behaves inconsistently. Fix all three.


Positioning and elevation:

The FAB should float vertically so roughly the top half sits above the tab bar and the bottom half overlaps into it (similar to the reference screenshot where the green scan button sits centered and elevated).
Use absolute positioning anchored to the horizontal center of the tab bar.
Add a subtle shadow (iOS: shadowColor: '#000', shadowOffset: {width: 0, height: 4}, shadowOpacity: 0.3, shadowRadius: 8; Android: elevation: 8).
Ensure the FAB has a higher zIndex than the tab bar so it renders on top.


Styling specifics:

FAB background color: the same orange used elsewhere in the app (looks like #F59E0B or similar — match the existing orange accent color).
Camera icon centered inside, white, roughly 28px.
Tapping the FAB should trigger the same camera action as before.


Do not change the dark theme, the tab icons, tab labels, or the active-tab color (blue for the selected tab). Only restructure the layout and fix the FAB shape/position.

Please make all these changes in one update and show me the updated component file.